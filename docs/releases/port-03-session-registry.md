# PORT-03 commerce-session route contract

September12, 2026. Pure contract authored through exact OpenCode
`opencode-go/deepseek-v4.1-flash`, reviewed by the lead. All 24 focused tests,
979 shared tests, build, type checking and focused lint passed, exit0.

The separate session manifest fixes seven routes and two audiences: five human
management routes and two agent exchange/status routes. Its ordered inventories,
dependency tuples, shared availability state and nested constants are strict and
immutable. Existing commerce, marketplace and policy registries are unchanged.

This does not register a live endpoint, activate sessions or grant payment
authority. Runtime, transport, database integration and browser acceptance are
separate pending work. Network remains explicit Arc Testnet, not guessed mainnet.
